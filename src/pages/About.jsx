import Reveal from '../components/Reveal';import Seo from '../components/Seo';import Crosshairs from '../components/Crosshairs';export default function About(){return <div className="container inner-page"><Seo title="About — Codevenient Consulting"/><Reveal><p className="label">Sheet 02 — About</p><h1>Built by people who care about the system behind the screen.</h1><p className="lead">Codevenient exists because small businesses often don't need more software — they need the right digital structure around how they already work.</p></Reveal><section className="section inner-section"><div className="sheets-grid">{[['01','Plan properly','Every build starts with the customer journey and the real operation behind it.'],['02','Build in the open','You see progress, make decisions early and avoid surprise handovers.'],['03','Reuse intelligently','Ready systems let proven patterns do the repetitive work so budget goes into what is unique.']].map(x=><div className="sheet" key={x[0]}><Crosshairs/><span className="sheet-tag">Fig. {x[0]} — How we work</span><h3>{x[1]}</h3><p>{x[2]}</p></div>)}</div></section></div>}
